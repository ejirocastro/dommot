import { CreateListing } from '@/components/host';

export default function CreateListingPage() {
  return <CreateListing />;
}

export const metadata = {
  title: 'Create Listing | Dommot',
  description: 'Create your property listing on Dommot',
};