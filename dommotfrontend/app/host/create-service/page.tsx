import { CreateService } from '@/components/host';

export default function CreateServicePage() {
  return <CreateService />;
}

export const metadata = {
  title: 'Create Service | Dommot',
  description: 'Create your professional service on Dommot',
};